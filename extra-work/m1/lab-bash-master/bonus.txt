name="Alvaro"
echo $name
mkdir $name
#ls -l
rm -r $name
#ls -l

for f in lorem/*; do
#echo $(basename $f)
filename=$(basename $f)
short="${filename%.*}"
echo "$short"
echo -n  "$short" | wc -c
echo "$short has $(echo -n "$short" | wc -c) characters length"
done

  
#tar -czvf name-of-archive.tar.gz /path/to/directory-or-file
tar -czvf lorem-compressed.tar.gz  lorem lorem-copy
mkdir lorem-uncompressed
tar -xvf lorem-compressed.tar.gz -C lorem-uncompressed
 
